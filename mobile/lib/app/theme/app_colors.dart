import 'package:flutter/material.dart';

/// Navy palette preserved from the reference Apps Script application.
/// The Branding capability may override these at runtime as it is built.
class AppColors {
  AppColors._();

  static const Color navyPrimary = Color(0xFF001F3F);
  static const Color navyDark = Color(0xFF001529);
  static const Color navyLight = Color(0xFF003366);
  static const Color navyAccent = Color(0xFF0074D9);
  static const Color navyHover = Color(0xFF002A52);

  static const Color textPrimary = Color(0xFF333333);
  static const Color success = Color(0xFF34A853);
  static const Color warning = Color(0xFFFBBC04);
  static const Color danger = Color(0xFFEA4335);

  static const Color surface = Color(0xFFF5F5F5);
}
